from setuptools import setup

setup(name='maxkmlgenerator',
      version='0.1.1',
      description='provides a module for kml generation',
      author='Gerry Agluba',
      packages=['maxkmlgenerator'],
      zip_safe=False)